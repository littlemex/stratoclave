# Stratoclave Backend Configuration

# ================================================================
# Server
# ================================================================
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=development  # development, production, local

# ================================================================
# CORS
# ================================================================
CORS_ORIGINS=http://localhost:3000,http://localhost:3003

# ================================================================
# Authentication
# ================================================================
# AUTH_MODE: none | apikey | shadow | oidc
# [WARNING] AUTH_MODE=none は production 環境では起動拒否される
AUTH_MODE=none

# OIDC Configuration (AUTH_MODE=oidc の場合に必須)
# OIDC_ISSUER_URL=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXX
# OIDC_AUDIENCE=your-client-id

# Cognito Configuration (Admin API でのユーザー管理用)
# COGNITO_USER_POOL_ID=us-east-1_XXXXX

# Auth0 Configuration (未実装 - スタブ)
# AUTH0_DOMAIN=your-domain.auth0.com
# AUTH0_CLIENT_ID=your-client-id
# AUTH0_CLIENT_SECRET=your-client-secret

# ================================================================
# Multi-Tenant
# ================================================================
TENANT_ISOLATION_ENABLED=false

# AWS Verified Permissions (TENANT_ISOLATION_ENABLED=true の場合に推奨)
VERIFIED_PERMISSIONS_ENABLED=false
# VERIFIED_PERMISSIONS_POLICY_STORE_ID=ps-xxxxxxxx

# ================================================================
# Database (PostgreSQL)
# ================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=stratoclave
POSTGRES_USER=stratoclave
POSTGRES_PASSWORD=your-password-here

# Redis (オプション - 将来のセッションストレージ用)
# REDIS_URL=redis://localhost:6379/0

# ================================================================
# AWS Bedrock Configuration
# ================================================================
# AWS_PROFILE=default
# AWS_DEFAULT_REGION=us-east-1
# AWS_REGION=us-east-1
# BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-5-v2:0
# BEDROCK_ENDPOINT_URL=https://localhost:8443  # Proxy Gateway 経由の場合
# BEDROCK_VERIFY_SSL=false  # Proxy Gateway でのローカル開発時のみ false

# ================================================================
# OpenClaw Integration
# ================================================================
OPENCLAW_ENABLED=true
OPENCLAW_GATEWAY_URL=ws://localhost:18789
OPENCLAW_AUTH_TOKEN=your-openclaw-token-here
