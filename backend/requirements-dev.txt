# Development + test dependencies, layered on top of requirements.txt.
# The production Docker image does NOT install this file, keeping the
# attack surface of the container small.
#
# Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Testing
# pytest 9.0.3 fixes CVE-2025-71176 (path traversal in tmp_path).
# pytest-asyncio 1.4.0 is the first release whose dist-deps allow
# pytest 9 (the 0.24.x line caps at pytest<9).
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# AWS SDK mock for backend tests
moto[dynamodb,cognitoidp,sts]==5.2.2
# moto[cognitoidp] uses joserfc to sign its fake JWTs.
# joserfc 1.6.3 fixes CVE-2026-27932 (CPU exhaustion via unbounded
# PBES2 p2c parameter).
joserfc==1.7.1

# Linting / formatting
ruff==0.8.4

# Formal / property-based tests for the pooled-billing logic.
# test_billing_formal_z3.py proves the concurrency invariants with an SMT
# solver (all interleavings, not examples); test_billing_stateful.py fuzzes
# the reserve/settle/release/reap/set-limit lifecycle with Hypothesis against
# billing/ledger.py (the executable spec of the DynamoDB money logic).
z3-solver==4.13.0.0
hypothesis==6.112.1

# Parses the machine-readable coverage.yaml each workshop scenario carries
# (scenarios/_schema/coverage.py). Dev/CI only — the workshop tooling never
# runs in the production image. Present transitively already; pinned here so
# the scenarios coverage aggregation has a declared dependency.
PyYAML>=6.0
